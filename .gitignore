# Dependencies
node_modules/

# Build output
dist/

# Electron build artifacts
out/

# CA certificates generated at runtime
certs/

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# IDE / editor
.vscode/
.idea/

# Claude Code session data
.claude/
.agents/
skills-lock.json

# Downloaded ML models (large binaries — re-downloaded on first use)
models/*
!models/ai-source-detector-onnx/
!models/clip_video_probe.json

# Training datasets (large — not shipped)
data/

# Training cache
cache/
embeddings/

# Python bytecode
__pycache__/
*.py[cod]

# Chrome-generated when loading unpacked extension locally
extension/_metadata/

# Packaged extension zip (build artifact)
*.zip

# User app data written at runtime (repo root only — not model config)
/config.json
counts.json
settings.json

# Secrets (Hugging Face token for dataset downloads)
.hf_token

# Tests and evaluation scripts
test/
__mocks__/
eval/

# Landing page lives in its own repo (slopblock-website)
landing-page/

seo/

solelanding.html

.env*
.env